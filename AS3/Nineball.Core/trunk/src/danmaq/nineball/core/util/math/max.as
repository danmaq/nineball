package danmaq.nineball.core.util.math
{
	
	/**
	 * 最大値を取得します。
	 *
	 * @param expr1 対象値。
	 * @param expr2 対象値。
	 * @return 最大値。
	 */
	public function max(expr1:Number, expr2:Number):Number
	{
		return expr1 > expr2 ? expr1 : expr2;
	}
}
